#include<stdio.h>
int main(){
//check for palindrome using two string
/*	char s1[100],s2[100]; int i,j;
	puts("Enter the string ");gets(s1);
	for(i=0;s1[i]!='\0';i++); //strlen
	for(j=0,s2[i--]='\0';i>=0;i--,j++)s2[j]=s1[i];
	for(i=0;s1[i]!='\0';i++)
	{
		if(tolower(s1[i])!=tolower(s2[i])){
			puts("Not a Palindrome");return;
		}
	}
	printf("palindrom");   */
}
