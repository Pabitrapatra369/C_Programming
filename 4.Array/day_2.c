#include<stdio.h>
#include<limits.h>
int main(){
	//Read nelements in to array and find the max, min elements
/*	int a[100],n,i,j,min=INT_MAX,max=INT_MIN;
	printf("Enter the range of array:");scanf("%d",&n);
	puts("enter the elements:");
	for(i=0;i<n;i++)scanf("%d",&a[i]);
	for(j=0;j<n;j++){
		if(max<a[j])max=a[j];
		if(min>a[j])min=a[j];
	}
	printf("max=%d min=%d",max,min);
	*/
// Finding prime/composite no’s :
/*   int a[100],n,i,j,c;
   puts("Enter the range of array:");
   scanf("%d",&n);
   puts("enter the elements of array:");
   for(i=0;i<n;i++)scanf("%d",&a[i]);
    for(i=0;i<n;i++){	  
      for(c=0,j=1;j<=a[i];j++){
      	 if(a[i]==1){
      	 	printf("1 is not a prime/composite\n");
      	 	goto last;
		   }
   	     if(a[i]%j==0)c++;
      }
      if(c==2)printf("%d is prime.\n",a[i]);
      else printf("%d is composite.\n",a[i]);
      last:;
   } */
   
}
