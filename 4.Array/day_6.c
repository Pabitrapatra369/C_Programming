#include<stdio.h>
int main(){
//finding frequency of a element in an array
/*	int a[100],b[100],i,j,n,c;
	puts("Enter array size:");
	scanf("%d",&n);
	puts("Enter array elements:");
	for(i=0;i<n;i++)scanf("%d",&a[i]);
	for(i=0;i<n;i++){
		if(b[i]==-1)continue;
		for(c=1,j=i+1;j<n;j++){
			if(a[i]==a[j]){
				c++;
				if(c>1)b[j]=-1;
			}
		}
		b[i]=c;
	}
	
	puts("frequency of element:\n");
	for(i=0;i<n;i++){
		if(b[i]==-1)continue;
		else{
			printf("%d found %d times\n",a[i],b[i]);
		}
	}
	*/
	
}
